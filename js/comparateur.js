//------------------
// LES OPERATEURS LOGIQUES 

let prenom
let loginAge
let loginPrenom
let age


//l'operateur ET " && " 

// si je cumule 2 conditions
// si prenom === loginPrenom ET === loginAge

if( (prenom === loginPrenom) && (age === loginAge)){
    // si ces 2 conditions sont remplis alors ici le code s'exécute
}

if ( a && b ){
    
}

//l'operateur OU " || "

if( a || b){ // ici il faut au moins une des 2 conditions pour que le code s'execute 
    
}  

//l'operateur " ! " signifie le contraire

let userIsLogin=true

if(!userIsLogin){// est différent de true 
    // si le user n'est pas connecté, si userIsLogin est false

}

// ce qui revient à ecrire

if(userIsLogin===false){
    // si le usesr n'est pas connecté, si userIsLogin est false
}

